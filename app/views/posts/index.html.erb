<div class="container pt-3">

  <!-- 掲示板一覧 -->
  <div class="row">
    <div class="col-12">
      <div class="row">
        <% if @posts.present? %>
          <%= render @posts %>
        <% else %>
          <p class="text-gray-500">投稿がありません</p>
        <% end %>
      </div>

      
    </div>
  </div>
</div>