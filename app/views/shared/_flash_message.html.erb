<% flash.each do |key, value| %> <!-- フラッシュメッセージのスタイルをカスタマイズ -->
  <div class="<%= key == 'notice' ? 'bg-green-500' : 'bg-red-500' %> text-white text-sm font-bold px-4 py-3" role="alert">
    <%= value %>
  </div>
<% end %>
